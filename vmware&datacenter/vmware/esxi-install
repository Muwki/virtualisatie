esxi install
via dcui of idrac
via dcui (direct console user interface)
plaats installatie media op de hardware
bij startup druk f11 om boot order te regelen


via idrac (integraded dell remote acces controller)
connect to idrac setup network en password
logon to idrac
power on server
launch virtual console
f2 to check system setup
system bios
boot settings
uefi boot settings, bootmode uefi
finish, finish,

select bootmanager f11
set boot menu ( drive vanuit je gaat installeren)
on time uefi boot menu
do one time boot en select de media
shift + voor special boot options


dcui install
gewoon stappen volgen, f11 to continue etc.
select disk use arrow keys
in de dcui is de disk volgorde van boven naar beneden.

bij disk veruiming in VMware gui 
is de disk volgorde van onder naar boven
provide root psw

dcui startup configs
f2
login
root en psw
use arrow keys to toggle

config managenetw
enter , netw adapt enter, lists of adaptrs, vmnic0 (default)
esc to exit

if 802 tag for vlan taggin
toggle to vlan , enter, set vlan nr
esc to go back

ip4 config
set ip4 op static (spacebar to select option)
enter

ipv6
disable ip6

dns config
obtain dns server (primary and secundary)

host name zet je bij dns settings
hostname wordt ook in deze sectie geplaats
vb. mur.esxi.com of laat standaard op localhost

bij custom dns suffix zet je het laatste gedeelte van je hostname
vb. esxi.com
optional

f2 
optie test network
to test if ip and dns are reachable.

test browser via links:
https:// ip adres
of 
via https:// hostname 
if dns is ok.


